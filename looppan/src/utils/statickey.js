const jwtToken = "jwtToken";

const category = {
  folder: 0,
  video: 1,
  audio: 2,
  image: 3,
  document: 4,
  other: 5,
};

const folderType = {
  file: 0,
  folder: 1,
};

const upload = {
  time: 0,
};

export default {
  jwtToken,
  category,
  folderType,
  upload,
};
